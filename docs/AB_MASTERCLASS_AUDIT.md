# AB Masterclass audit

## What this is

Harbormaster graded against the sixteen system-design principles of the Arpit Bhayani System Design Masterclass (dimensions D1-D16, cross-checked against the `ab-system-design` note set). It is a design-review artifact meant to be read first by a skeptical principal engineer or an international/defense buyer: it says plainly where the platform is production-shaped, where it is only documented, and where it deliberately builds nothing.

**How it was produced.** A read-only multi-agent audit distilled the masterclass into a sixteen-dimension rubric, then walked the Harbormaster codebase against it. Every high-stakes finding was hand-verified against the actual code at the cited `file:line`. Two honesty rails govern every row: a capability that is documented but never run caps at **Partial** (a "COMPLETED" design is not a result), and an item that `docs/HONESTY.md` declares out of scope is marked **Out-of-scope-by-design**, not a gap.

**The net grade.** Harbormaster is a genuinely well-engineered, honestly-scoped personal platform. Its **CDC plane is production-shaped**: real Postgres logical replication, an idempotent LSN-guarded sink, and a property-tested convergence model now backed by a hermetic 2-partition rebalance proof. Its pure logic across the lake, serving, and MLOps planes is densely tested. The original audit flagged five load-bearing clusters; the `feat/ab-masterclass-audit` branch closed the local-correctness ones (lakehouse compaction/idempotency, streaming DLQ + backpressure, SLO burn-rate signal, cache stampede, credibility artifacts, IaC/CI gates) and authored the security front door (API Gateway hardening + IAM boundary + resource scoping). What remains is genuinely apply-time, not-yet-authored, or out-of-scope-by-posture: streaming real event-time windowing (deferred per ADR 0001), the MSK live run, the two-variant live canary, the `terraform apply` of the IAM boundary + apigw hardening (authored, not applied), and the customer-managed KMS key (`modules/kms`, authored behind `enable_cmk = false`, never applied). See the Remediation status section below for the per-phase map. Authored-not-applied infrastructure stays Partial by rule, never Solid.

This document cross-links each dimension to its decision record in `docs/SYSTEM_DESIGN_DECISIONS.md` and does not restate the reasoning there. It adds the one column that catalog lacks: **run versus documented**.

---

## Remediation status (branch `feat/ab-masterclass-audit`, 2026-07-06)

This branch worked the top-remediation list against the audit above. Statuses in the matrix that follow reflect the post-remediation reality. The honesty rails still hold: authored-but-not-applied infrastructure caps at **Partial**, never **Solid**, and no AWS-spend or live-run claim is made from code that only exists on this branch. Full suite after the branch: **434 passed, 9 skipped**.

**Phase 0 (1a09f14): credibility artifacts.** This audit doc, four ADRs (`docs/adr/0001`-`0004`), the Bedrock and OTel doc-drift fixes (`ARCHITECTURE.md`, `PHASE_1.md`), and the P1-P8 war-story ANTICIPATED-vs-GROUNDED tags. Closes the (c) credibility bucket.

**Phase 1 code (5cb5a01):** Kinesis PutRecords exponential backoff with full jitter (`ingest.py`); watchlist single-flight coalescing (`watchlist.py`); streaming pure-logic extracted to `window_logic.py` with tests; RDS slot-lag Lambda TLS hardened (removed `CERT_NONE`, now `CERT_REQUIRED` + optional CA-bundle pin).

**Phase 1 gates (5439d7b):** real pre-commit + bandit; coverage ratchet (`fail_under=75`, measured 78.6%, 90% standard on new/changed modules); `iac-ci.yml` with tflint + checkov baseline; a local M4 Pro score benchmark (p95 ~0.58 ms, ~1,787 scores/sec/core).

**Phase 2A (69b8d1a):** Google-SRE multi-window burn-rate calculator (`serving/app/burn_rate.py`) wired into `slo.py` and `mlops/promote.py:make_burn_check`, plus observability composite alarms; Iceberg partition spec + upsert idempotency + compaction/`expire_snapshots` (`lake/iceberg.py`); streaming DLQ/quarantine + bounded scorer retry + shard-skew instrumentation; a hermetic 2-partition rebalance proof (`cdc/tests/test_applier_multipartition.py`).

**Phase 2B authored (45d221c):** IAM permissions-boundary + resource-scoped management closing the `iam:*` on `Resource: *` escalation; API Gateway `AWS_IAM` (SigV4) default authorizer + throttling + access logging + gated WAF; honest checkov baseline refresh. Terraform-authored, **not applied**, so it stays **Partial**.

**Still open (not closed on this branch):**
- Streaming real event-time windowing / checkpointing. Deliberately kept per-event per ADR 0001; a true tumbling window is future work.
- MSK / real multi-partition live run. The rebalance proof is hermetic; the managed-broker path still never ran.
- Two-variant live canary. Authored, not applied: `mlops/canary_actuator.py`'s `make_set_canary_weight` now shifts BOTH variants in one `UpdateEndpointWeightsAndCapacities` call, and `modules/sagemaker_pidpm` has the count-gated `candidate` production variant (zero-diff default) behind `candidate_model_data_url` (Wave 1 T2, commit `64cbd3c`); only the live apply + weight-shift window remains.
- Apply of the IAM permissions-boundary and API Gateway hardening. Both authored in Terraform, neither applied.
- Customer-managed KMS keys. Authored, not applied: `modules/kms` (rotation-enabled key + alias, wired to the S3 buckets, RDS storage, DynamoDB tables, and every CloudWatch log group behind `enable_cmk`, default false with a zero-diff plan); needs an apply window before it counts.
- Real multi-region DR. Single-region us-east-1 remains the declared posture; RPO/RTO documented (ADR 0003), not built.

---

## The matrix

Status legend: **Solid** (built and tested), **Partial** (real but incomplete, or documented-not-run, or authored-not-applied), **Gap** (missing against the bar), **Out-of-scope-by-design** (declared out in `docs/HONESTY.md`, not counted as a gap). Authored infrastructure that only exists on this branch and has never been `terraform apply`-ed caps at **Partial** by the same rail that caps documented-not-run at Partial. The ★ dimensions are the six load-bearing walls for a streaming + CDC + lakehouse + ML-serving platform; each cites the grounding AB note.

| Dimension | DR entry | Status | Run vs Documented | Evidence (file:line) | Remediation pointer |
|---|---|---|---|---|---|
| **D1. Capacity estimation** ★ (AB note `01`/`02`/`10`/`13`) | DR-14 | Partial | Score kernel: measured locally (extrapolated to fleet/$). Ingest/storage: Documented | `bench/SCORE_BENCH.md` + `bench/bench_score.py`: measured on Apple M4 Pro (local dev Mac, arm64), single-event golden path p95 ≈ 0.61 ms, ≈ 1,760 scores/sec/core, $/inference ≈ 3e-9 (extrapolated onto the `cost.py` Fargate rate, not a measured cloud figure); design estimates `SYSTEM_DESIGN_DECISIONS.md:297-334` (10K/30K msg/s, 30-40 shards, 12-15 TB/yr), honesty caveat `:330`; `EXPERIENCE_REPORT.md:73`; `PLATFORM_BOOK.md:341` | Top remediation (b)5: DONE for the score kernel (`bench/SCORE_BENCH.md`); remaining = a cloud-instance load test to replace the $/inference extrapolation with a measured AWS figure, plus an ingest/storage throughput test |
| **D2. Single-node performance** | (no dedicated DR) | Solid | Run (tested) | `features/features.py`, `orchestrator.py` deterministic CPU/NumPy path; golden `latency_ms < 200ms` `test_golden.py:27`; `slo.py:27` p95≤300 | Minor: add one `py-spy`/`cProfile` hot-path capture to `bench/` |
| **D3. Relational modeling, locking, indexing** | DR-4 (write model) | Partial | Run (upserts) / Documented (locking) | `registry.py`; `INSERT…ON CONFLICT` `hitl.py:132`, `registry.py:138`; `wal_level=logical` `10-postgres.yaml:23`; RLS design-only `PLATFORM_BOOK.md:207` | ADR: state no fixed-inventory contention exists, OR add `SELECT … FOR UPDATE SKIP LOCKED` on HITL dequeue + test |
| **D4. Access-pattern-first / denormalization** ★ (AB note `04`/`08`/`16`) | DR-4 | Solid | Run (CQRS split live) | CQRS split `registry.py:1-6`, `watchlist.py:1-9`; point-in-time `merge_asof` `export_training_set.py:23` | Minor: add a read→store→key paragraph to `SYSTEM_DESIGN_DECISIONS.md` |
| **D5. Replication / consistency / CAP-PACELC** | DR-16, DR-1 | Partial | Run (LSN guard + hermetic rebalance proof) / Documented (MSK live) | CDC LSN guard `applier.py`, `sinks/base.py:68`; `ConsistentRead=False` `watchlist.py:11`; **rebalance now hermetically proven**: `cdc/tests/test_applier_multipartition.py` drives 2 partitions + a mid-stream consumer-group rebalance and asserts per-(table, pk) convergence; CDC staleness ADR `docs/adr/0002`; MSK managed-broker path still never ran `PLATFORM_BOOK.md:143` | Remaining: an MSK live multi-partition run to replace the hermetic proof with a real-broker figure |
| **D6. Partitioning / sharding** ★ (AB note `02`/`06`/`12`) | DR-9 | Partial | Run (key + skew instrumentation + lake partition spec) / Documented (hot-shard live mitigation) | MMSI→Kinesis key `ingest.py`→Flink `key_by`; **shard-skew instrumentation landed**: `ingest.py:157` `shard_of` + `:173` `shard_distribution` + `:183` `skew_ratio` quantify hot-shard risk (tested); **Iceberg partition spec landed** on the writer, day(t)+bucket(mmsi) intended `iceberg.py:125-146`, applied via `_resolve_partition_fields` `iceberg.py:160`; hot-shard live mitigation still anticipated (`PLATFORM_WAR_STORIES.md`) | Remaining: a live explicit-hash re-spread if a real hot shard appears; the skew metric is the trigger |
| **D7. Consensus / quorum / leader election / locks** | none (HONESTY.md scope-out) | Out-of-scope-by-design | N/A | `HONESTY.md:31-34` (no Raft, Paxos, or consensus; coordination via managed services) | None. Keep the disclaimer; optionally cite Kleppmann ch9 + Chubby in an ADR |
| **D8. Fault tolerance & failure detection** | DR-8 (async), DR-13 (detection) | Partial | Run (CDC/serving + streaming DLQ + scorer retry) / Documented (live S3 wiring) | Slot-lag Lambda `treat_missing_data="breaching"`; **malformed-AIS DLQ landed**: `window_logic.py:225` `quarantine_envelope` + `job.py:182` `_quarantine` write a self-describing envelope instead of a silent drop; **bounded scorer retry landed**: `window_logic.py:249` `post_scorer_with_retry` (capped exp-backoff, `on_error` callback, never raises), wired at `job.py:257`, dead-letters on give-up `job.py:264`; the S3 quarantine sink is authored and gated on a `quarantine_bucket` prop, so the live S3 write path is authored-not-run | Remaining: exercise the real S3 quarantine write in a live run |
| **D9. Distributed ID generation** | DR-15 (idempotency keys) | Solid | Run (tested) | Composite keys `"<mmsi>:<regime>"` with Postgres `CHECK` `ddl.py:97`; replay-stable `trace_id` drives HITL idempotency | None material. Optionally note in ADR why no Snowflake/ULID is needed |
| **D10. Storage engines: LSM / WAL / compaction / amplification** ★ (AB note `10`/`11`) | DR-5, DR-12 | Partial | Run (upsert idempotency + partition spec + compaction) / Documented (core-partition fallback caveat) | **Upsert idempotency landed**: `Table.upsert(join_cols=...)` on each table's natural key (ais_history mmsi+t, nodes node_id) replaces the re-run-doubling `table.append` `iceberg.py:15-19`; **partition spec landed** `iceberg.py:125-146`; **compaction + snapshot expiry landed**: `run_lake_maintenance` `iceberg.py:255` read-all-overwrite coalesce + `expire_snapshots`, returns before/after file+snapshot counts for tests. Caveat: the day/bucket transforms need the `pyiceberg_core` Rust extension; when absent, `_resolve_partition_fields` `iceberg.py:160` falls back to an identity partition on the first dedup key (documented single decision point, no silent downgrade) | Remaining: run maintenance on a real cloud lake with `pyiceberg_core` installed |
| **D11. High-throughput: batching / async / backpressure / exactly-once / idempotency** ★ (AB note `02`/`08`/`11`) | DR-2 (CDC), DR-6 (backpressure) | Partial | CDC: Run (Solid). Streaming: Run (backoff + DLQ) / Documented (real windowing) | CDC apply→flush→commit `applier.py:86-103`, 25-schedule convergence property test `test_applier.py:292`, `enable.auto.commit=False`; **Kinesis PutRecords backoff landed**: bounded exponential + full jitter, retries only still-failing records `ingest.py:256` `_kinesis_putter` + `:106` `jittered_backoff` (tested); **DLQ landed** (see D8); streaming still per-event `KeyedProcessFunction`, no event-time window/watermark/checkpoint, a deliberate choice per ADR 0001 (`job.py:38-42`) | Remaining (per ADR 0001): real event-time windowing + checkpointing against a Flink mini-cluster if the per-event realization is ever outgrown |
| **D12. Caching: patterns / invalidation / hot keys / stampede** | DR-4, DR-16 | Partial | Run (cache-aside + single-flight coalescing) / Documented (negative cache deliberately skipped) | Cache-aside write-invalidate `redis_cache.py`; read-through TTL, fail-open; **single-flight coalescing landed**: concurrent cache-miss reads for the same MMSI share one in-flight fetch via an `asyncio.Future` map guarded by a loop-bound lock `watchlist.py:171-266` (tested `test_watchlist.py`), so a stampede on one hot vessel hits DynamoDB once. Negative cache deliberately skipped: the workload is watchlist-membership lookup where misses are the common, cheap case and a negative TTL would add invalidation complexity for little gain; the coalescing already bounds the miss fan-out | Remaining: revisit a negative cache only if a real miss-storm shows up in metrics |
| **D13. Information retrieval / search / indexing** | none (N/A for this workload) | Out-of-scope-by-design | N/A | No search surface; workload is point-lookup + scoring, not corpus search | None. Stated N/A: single-vessel scoring, not IR |
| **D14. Rate limiting / load shedding / throttling** ★ (AB note `12`/`16`) | DR-6 (P_phys shed) | Partial | Run (corrupt-gate) / Documented (apigw hardening authored, not applied) | Corrupt-gate 422 `orchestrator.py:327`; **API Gateway hardening authored** in `modules/apigw/main.tf`: `AWS_IAM` (SigV4) default authorizer so the public route is not anonymously open `main.tf:20-28,92-95`, always-on stage throttling (rate + burst) `main.tf:115-116`, access logging `main.tf:100-103`, gated WAF, JWT authorizer option. Terraform-only, **not applied** | Remaining: `terraform apply` the apigw module; add a bounded scorer queue for load shedding |
| **D15. Observability & operability** | DR-13 | Partial | Run (dashboards/alarms + burn-rate calculator) / Documented (OTel not built) | HITL/task lifecycle columns; CloudWatch dashboard + SLO alarms + composite alarms (`modules/observability/main.tf`); **burn-rate calculator built**: `serving/app/burn_rate.py` Google-SRE multi-window burn-rate (`evaluate_burn_rate` `:194`, tiered thresholds 14.4/6/3, `should_rollback`), tested `test_burn_rate.py`, wired into `slo.py`; OTel/X-Ray still not built (doc-drift corrected in Phase 0 to name it a planned gate, not implemented) | Remaining: build OTel spans (currently correctly labeled a planned gap, not a false claim) |
| **D16. Algorithmic design: probabilistic structures / reduction / geo-indexing** | none (no dedicated DR) | Partial | Run (clustering) / Documented-optional (GeoHash/bloom deliberately not built) | HDBSCAN waypoint clustering + RDP `transforms.py`. GeoHash/bloom deliberately NOT built on this branch: with the watchlist miss fan-out already bounded by single-flight coalescing (D12) and no corridor/proximity search surface in the current workload, a GeoHash index or a negative-lookup bloom would be speculative dead code. Left as a documented-optional to add only when a real proximity-search or miss-storm requirement appears | Optional (not a gap): GeoHash prefix index if a corridor/proximity query surface is added; bloom if watchlist misses become a measured hot path |

### Cross-cutting infra / security / governance

Folded from the gap matrix and graded against the principal-engineer-plus-international-buyer bar the review sets.

| Dimension | DR entry | Status | Run vs Documented | Evidence (file:line) | Remediation pointer |
|---|---|---|---|---|---|
| **Least-privilege / IAM blast radius** | DR-7 (default-deny, tenant) | Partial | Documented (boundary + scoping authored, not applied) | **Escalation closed in code**: `iam:CreateRole/AttachRolePolicy/PutRolePolicy` now scoped to `arn:aws:iam::*:role/harbormaster-*` and conditioned on the `harbormaster-permissions-boundary` (`harbormaster-platform-permissions.json:7-52`); the boundary itself (`harbormaster-permissions-boundary.json`) denies removing/altering the boundary and org escape (`:91-97`). Replaces the old `iam:*` on `Resource: *`. Authored, **not applied**, so Partial. **Apply-time prerequisite:** every module `aws_iam_role` MUST set `permissions_boundary = harbormaster-permissions-boundary`, or `CreateRole` is denied by the scoped policy condition | Remaining: `terraform apply` with the boundary attached on every role; verify no role is created without it |
| **DR / multi-region / backup** | DR-13 (posture context) | Out-of-scope-by-design (posture) / Gap (documentation) | Documented (RPO/RTO absent) | `multi_az=false`, `backup_retention=1`, `skip_final_snapshot=true` `rds/main.tf`; "entire footprint is us-east-1" `PLATFORM_BOOK.md:151` | Top remediation (b)4: RPO/RTO + DR-scope ADR; do NOT claim DR you don't have |
| **Encryption / KMS / data residency** | (no dedicated DR) | Partial | Run (SSE + TLS verify) / Documented (CMK) | S3 SSE-AES256, RDS `storage_encrypted`, DynamoDB PITR; **`CERT_NONE` removed**: the slot-lag Lambda now builds a verifying SSL context (`check_hostname` True, `CERT_REQUIRED`) with an optional `RDS_CA_BUNDLE` pin `cdc_slot_lag/handler.py:40-52`; CMK now authored (`modules/kms` behind `enable_cmk = false`, wired to S3/RDS/DynamoDB/log groups), not applied | Remaining: apply the CMK path (`enable_cmk = true`) in a demo window, not just default SSE |
| **IaC governance / CI** | DR-11 (phased build) | Solid | Run (gates green) | **Gates now real and passing**: `.github/workflows/iac-ci.yml` runs tflint + checkov against a committed baseline (`infra/terraform/.checkov.baseline`); pre-commit + bandit configured; coverage gate live in `pyproject.toml` (`fail_under=75` ratchet, measured 78.6% line+branch; 90% standard on new/changed modules); serving CI (`serving-ci.yml`). Full suite 434 passed, 9 skipped | Minor: ratchet `fail_under` upward as coverage climbs; add `terraform plan` on a real backend once account wiring is de-hardcoded |
| **Promotion actuators / burn-rate signal** | DR-3, DR-13 | Partial | Run (real burn signal) / Documented (two-variant live canary) | **`burn_check` now consults the real calculator**: `mlops/promote.py:make_burn_check` `:114` builds a `burn_check(weight) -> bool` from `evaluate_burn_rate` (the DR-13 multi-window calculator), replacing the hardcoded `False`; `run_promotion` `:42` still takes it injected for testability (tested `test_promote.py`). Actuator side now AUTHORED (Wave 1 T2, `64cbd3c`): `mlops/canary_actuator.py:make_set_canary_weight` shifts both variants in one call and `modules/sagemaker_pidpm` carries the count-gated `candidate` variant, so only the live apply remains | Remaining: apply the two-variant SageMaker endpoint and run a live weight shift; the actuator and the burn signal that gates it are both ready |

---

## What we deliberately do not build, and why

The point of this section is that a skeptical reader never has to guess whether something is missing or scoped out. Each item below is a deliberate boundary, stated up front.

- **Consensus (Raft / Paxos) and a sharded query router.** `HONESTY.md:31-34` declares both explicitly out of scope: "Harbormaster does not implement query sharding or a routing layer across shards" and "does not implement Raft, Paxos, or any consensus protocol. Where coordination is needed it uses managed services, and that reliance is stated plainly." This is Vitess/Multigres territory; Harbormaster consumes managed Postgres and managed brokers rather than re-implementing coordination. If asked whether the platform proves a consensus system or a sharded router, the honest answer is no (D7 above; `HONESTY.md:34`).
- **Multi-region DR as a running system.** Single-region us-east-1 is a declared cost posture on a $75/month account (`multi_az=false`, `backup_retention=1`), not an oversight. The real gap is documentation, not architecture: the RPO/RTO numbers must be written down even though the answer is "single-region by design." A buyer needs the numbers; do not claim DR that does not exist.
- **The phase3-demo-standin model.** The SageMaker endpoint served a labeled `phase3-demo-standin` throughout the live showcase, never a real trained checkpoint (`HONESTY.md:63`). The claim supported is the infrastructure and promotion discipline, not model quality. The label stays.
- **Simulated personas, clients, and FDE scenarios.** Every customer narrative carries an inline `SIMULATED` label per the `HONESTY.md:41` labeling rule. These are never presented as real engagements.
- **Information retrieval / search (D13).** N/A for this workload: Harbormaster does single-vessel scoring and point-lookup serving, not corpus search. No Elasticsearch surface is built, and none is needed. Stated, not silently absent.
- **GeoHash spatial index and negative-lookup bloom filter (D16).** Deliberately not built on the remediation branch, not an oversight. The watchlist miss fan-out is already bounded by single-flight coalescing (D12), and the current workload has no corridor or proximity search surface, so a GeoHash prefix index or a bloom on negative lookups would be speculative dead code. Both are documented-optional: add a GeoHash index when a real proximity-query surface exists, add a bloom when watchlist misses become a measured hot path. No speculative structure was added to inflate the algorithmic-design row.

---

## Top remediations

Mirrors the three gap-matrix buckets. Each item points at the phased plan in the gap matrix (Part 2); none recommends *claiming* a capability, only building it locally, proving it with spend, or labeling it out loud.

### (a) Real correctness / robustness (local, no spend)

1. **Streaming DLQ + backpressure: DONE; real windowing still deferred.** Kinesis putter now uses bounded exp-backoff + full jitter (`ingest.py:256`), a real quarantine/DLQ sink and bounded scorer retry landed (`window_logic.py:225,249`, `job.py:182,257`), and `window_logic.py` is unit-tested. Still open: `job.py` keeps the per-event `KeyedProcessFunction` (no window/watermark/checkpoint) as a deliberate choice per ADR 0001; real event-time windowing is future work. (D8, D11.)
2. **CDC idempotency ordering: hermetic multi-partition proof DONE; MSK live still open.** The per-key LSN guard is now proven under 2 partitions + a mid-stream rebalance in `cdc/tests/test_applier_multipartition.py`. Still open: a real MSK managed-broker run. (D5, D6.)
3. **Lake idempotency + compaction: DONE.** `Table.upsert` on natural keys replaces the re-run-doubling `table.append`; partition spec + `run_lake_maintenance` compaction + `expire_snapshots` landed (`iceberg.py:255`). Note the `pyiceberg_core` identity-partition fallback when the Rust extra is absent (`iceberg.py:160`). (D6, D10.)
4. **Cache stampede: DONE.** Per-key single-flight coalescing on watchlist misses landed (`watchlist.py:171-266`, tested). Negative cache deliberately skipped with reason (miss is the cheap common case; coalescing already bounds fan-out). (D12.)

### (b) Production guarantees

1. **SLO burn-rate + error-budget alerting: built and wired.** A Google-SRE multi-window burn-rate calculator (`serving/app/burn_rate.py`, tested) now feeds `slo.py` and `mlops/promote.py:make_burn_check`, so `burn_check` consults a real signal instead of a hardcoded `False`. Still open: the LIVE apply of the two-variant canary (the actuator `mlops/canary_actuator.py` and the count-gated `candidate` variant are authored as of Wave 1 T2 `64cbd3c`; only the apply + live weight shift remain). (D15; promotion actuators.)
2. **API Gateway hardening: authored, not applied.** `AWS_IAM` (SigV4) default authorizer + throttling + access logging + gated WAF landed in `modules/apigw/main.tf`. Terraform-only, needs `terraform apply`; stays Partial until applied. (D14.)
3. **IAM blast radius: escalation closed in code, apply pending.** `iam:*` on `Resource: *` replaced by role-scoped management under `harbormaster-*` conditioned on the permissions boundary (`harbormaster-platform-permissions.json`, `harbormaster-permissions-boundary.json`). Apply-time prerequisite: every module `aws_iam_role` must set `permissions_boundary`, or `CreateRole` is denied. Stays Partial until applied. (IAM blast radius.)
4. **DR / residency + TLS: ADR written, cert fix DONE.** ADR 0003 states single-region RPO/RTO; the slot-lag Lambda `CERT_NONE` is removed for verifying TLS (`cdc_slot_lag/handler.py:40`). Still open: applying the CMK (`modules/kms` authored behind `enable_cmk`, default false); real multi-region DR is out of scope by posture. (DR/multi-region; encryption/KMS.)
5. **Capacity planning: local benchmark DONE; cloud load test open.** A committed M4 Pro score benchmark (p95 ~0.58 ms, ~1,787 scores/sec/core) grounds the kernel figure; a measured cloud $/inference load test remains. (D1.)

### (c) Credibility / documentation (completed in this pass, 2026-07-06)

1. **This file, `docs/AB_MASTERCLASS_AUDIT.md`,** is the first-class design-review artifact folding the matrix in as a repo deliverable; it cross-references `SYSTEM_DESIGN_DECISIONS.md` without restating it and cites AB notes for the load-bearing six.
2. **ADRs** in `docs/adr/` for the previously-implicit decisions: streaming realization (per-event vs windowed), CDC staleness budget, single-region DR (RPO/RTO), no-consensus/no-sharded-router (cites `HONESTY.md`). Each ADR closes a documented-not-demonstrated row.
3. **War stories tagged ANTICIPATED vs GROUNDED.** `PLATFORM_WAR_STORIES.md` mixed anticipated stories (P1-P8) with grounded ones; a buyer-grade artifact must not let anticipated stories read as observed. Each P1-P8 now carries a one-line tag, matching the `HONESTY.md` discipline.
4. **Two doc-drifts resolved:** Bedrock, described as current architecture in `ARCHITECTURE.md:71`, is now labeled a Phase-5 planned item consistent with `PLATFORM_BOOK.md`; the OTel tracing claimed in `PHASE_1.md:78` is now labeled a planned gate target that was not implemented (no OTel/X-Ray in the code). Both were credibility landmines under a skeptical read.

Where `SYSTEM_DESIGN_DECISIONS.md` already covers a dimension well (D1 estimation `:297-334`, D4/D16 CQRS/PACELC DR-4/DR-16, D11 CDC idempotency DR-2, D12 lambda/kappa DR-12), this audit links to those DR entries and adds only the run-versus-documented column the DR catalog lacks.
