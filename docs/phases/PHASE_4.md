# Phase 4: Drift -> HITL -> RL flywheel (ultra-refined execution plan)

Status: CODE-COMPLETE 2026-07-04 (24-hour completion sprint; signed off by Arun before gate 4.0, built on branch `phase4-flywheel` off `phase3-lake`, gates 4.0-4.7 all committed, one commit per gate). All acceptance criteria proven locally against pure functions and fakes (no live stack, matching Phase 3's precedent): `make phase4-e2e` is green (5/5), both drills (`make drill-l3-drift-classification`, `make drill-l4-reward-hacking`) pass against the real code, full suite 375 passed / 9 skipped (post-adversarial-review count, see addendum below), ruff clean, `make validate` clean. Gate 4.6's Terraform (`modules/drift_watch`) is plan-checksum-verified only (0 add at committed values; exactly 8 to add with `enable_phase1=true, enable_phase4=true`, no `-target` needed thanks to this sprint's `ecs_serving` fix), never applied. Everything else in this phase is local-plane: pure functions, injected I/O, fakes. Promotes `docs/phases/PHASE_4_SKETCH.md`'s three locked decisions (drift taxonomy, preference-triple schema, reward-hacking probe) into executable gates; the sketch stays in place as the design record. War stories P13+P14 appended to `PLATFORM_WAR_STORIES.md`. Remaining, explicitly out of scope this sprint (sprint-honest scope, below): the concept-drift disagreement-rate threshold is a named placeholder pending a real HITL baseline; `epistemic_variance`'s source (true EDL head vs ensemble proxy) is an MSI-training-time decision; the real MSI DPO/GRPO fine-tune; HITL corrected-trajectory capture; gate 4.6's Terraform apply. Governance: this doc is the working plan for Phase 4; any change updates this file AND the master plan (`~/.claude/plans/i-am-thinking-to-rustling-sifakis.md`) before executing. No scope drift mid-phase. `terraform apply` is Arun-run only; Claude does validate + plan + file edits + tests.

Anchor fact-check findings folded in at authoring time (2026-07-04, every reuse anchor re-read from source rather than trusted from the sketch):
- pi-grpo lives at `~/Library/CloudStorage/Dropbox/Claude Interview Prep Documents/repo-audit-work/pi-grpo/`, not `~/code/pi-grpo`; the sketch's anchors resolve there (`app/components/preference_builder.py`, `app/components/physics_reward.py`), verified on disk.
- pi-grpo's real `PreferencePair` is five fields (`prompt`, `chosen`, `rejected`, `margin`, `source`) plus `to_jsonl()`; the sketch's richer JSON object is Harbormaster's extension of that shape, not a copy of it. The port keeps pi-grpo's real semantics where they exist: `from_hitl_jsonl` accepts only `label in {"correct", "incorrect"}` (ambiguous excluded), `synthesize_from_reward(base_outputs, *, margin_min=0.5)` picks `chosen = argmax reward` and emits at most K-1 pairs per prompt with margin >= `margin_min`, `source` is `"hitl"` / `"synthetic"`.
- `RewardWeights(hard=5.0, soft=1.0, data=1.0, pref=1.0)` and `RewardBreakdown(total, hard, soft, data, pref)` with `to_panel()` confirmed verbatim; `PhysicsReward.score(states, *, pi_dpm_log_prob=None, pref_logit=None) -> RewardBreakdown`; `_hard_penalty` is genuinely unbounded above (sum of relative excesses), which is what makes the reward-hacking probe's premise real rather than rhetorical.
- MIRROR's `~/code/mirror/serving/monitoring/drift.py` confirmed: `DriftConfig(psi_warn=0.1, psi_alert=0.25, ks_pvalue_alert=0.05, calibration_tol=0.25, n_bins=10)`, `check_input_drift(reference, current, config) -> list[DriftResult]`, hand-rolled `ks_pvalue` via `_probks` (no scipy dependency; the port stays numpy-only, no new extra needed since numpy is already a core dependency).
- Harbormaster integration points confirmed: `hitl_queue` DDL (`id, trace_id UNIQUE, mmsi, ts, score, reasons JSONB, confidence, label TEXT, reviewer TEXT, created_at`), `FeedbackIn.label: Literal["correct", "incorrect", "ambiguous"]`, `anomaly_hitl_threshold: float = Field(0.6)` (`serving/app/config.py:51`), `run_promotion(*, holdout_result, shadow_result, burn_check, set_canary_weight, revert_to_champion)` (`mlops/promote.py:40`), `calibration_ratio(errors, variances)` (`mlops/holdout_gate.py:81`), `PiDpmClient.score/ascore -> float | None` parsing `float(payload["score"])` (`serving/app/pidpm_client.py:157`).
- Design simplification vs the sketch, decided here: NO `hitl_queue` schema migration in Phase 4. The sketch said "extended with the operator's chosen/rejected framing," but for the label-only flywheel the chosen/rejected framing is fully derivable from existing columns (`label` + `score` + `anomaly_hitl_threshold`: the model's implied verdict is `score >= threshold`, the operator's verdict is the label, and a disagreement yields chosen = operator side, rejected = model side). Corrected-trajectory capture, the one thing that would genuinely need new columns, is explicitly out of scope (below), so the migration would be dead surface this sprint. If/when trajectory capture lands, the schema extends then.

## Sprint-honest scope (cannot close in 24h, by design, stated up front)

- The concept-drift disagreement-rate alert threshold ships as a named, documented placeholder (`DISAGREEMENT_ALERT_RATE_PLACEHOLDER = 0.2`, chosen only so the mechanism is testable) with an explicit TODO tied to a real HITL baseline; the mechanism is real, the number is not yet.
- `epistemic_variance`'s source (true EDL/Dirichlet head vs ensemble-variance proxy) is an MSI-training-time decision; the client/payload contract ships compatible with either and with its absence (the field is optional end to end).
- The real MSI DPO/GRPO fine-tune is post-sprint; the preference pipeline ends at validated JSONL under `mlops/preference_data/` plus the S3 export command shape.
- Drift-alerting Terraform (gate 4.6) is authored + `validate` + plan-checksummed, never applied this sprint.
- HITL corrected-trajectory capture (frontend) is out of scope entirely.

## Gates

4.0 Branch + toggle scaffold (gate F0). Branch `phase4-flywheel` off `phase3-lake`. New Terraform variable `enable_phase4` (default `false`) in `envs/base`, no resources behind it yet (gate 4.6 adds the module); tfvars comment matching the `enable_phase2`/`enable_phase3` convention (demo-window-only framing plus what it will eventually gate). No new pyproject extra: every gate below is numpy/pandas-only at module level, both already available (numpy core, pandas via the `lake`/`mlops` install CI already performs), matching the lazy-import convention for anything heavier. Unit: none (scaffold). Smoke: `make validate` clean. Checksum: `terraform plan` with committed toggle values attributes zero diff to Phase 4 (nothing exists yet); full suite + ruff clean on the new branch.

4.1 Input drift (gate F1). New `mlops/drift.py`: a faithful numpy-only port (vendor, not cross-repo import) of MIRROR's `population_stability_index`, `ks_statistic`, `_probks`/`ks_pvalue`, `DriftConfig`, `DriftResult`, and `check_input_drift`, adapted to Harbormaster's feature-frame shape (the same per-MMSI feature columns the gate 3.3 training-set export produces; the reference window IS a training-set export's feature distribution, so the promotion pipeline and the drift monitor definitionally watch the same distribution). Rolling-reference decision, locked: reference = the feature columns of the most recent accepted training-set export (a parquet snapshot alongside the gate 3.3 export output), current = a caller-supplied window (the scheduled job that supplies it for real is gate 4.6's Lambda; locally the drills supply it). Unit: synthetic undrifted fixture (same distribution, alert-free at the default thresholds) and synthetic drifted fixtures (mean-shifted and variance-inflated, PSI and/or KS alerts fire); boundary tests on the `psi_warn`/`psi_alert` tiering. Cross-check, per the sprint's verify-against-independent-reference discipline: expected PSI/KS/p-values for the committed fixtures computed once from MIRROR's original module (dev-time, outside the test run) and pinned as constants in the test with a provenance comment, so the port is proven numerically equivalent, not assumed. Smoke: `make drift-smoke` runs `check_input_drift` on the committed fixture pair and prints the tiered results. Checksum: SHA256 of the fixture pair + pinned expected values in `mlops/fixtures/drift_expectations.json`.

4.2 Calibration drift (gate F2). New `mlops/calibration_watch.py`: `watch_calibration(labeled_outcomes, *, band=(0.8, 1.2)) -> CalibrationWatchResult` re-CALLING `mlops.holdout_gate.calibration_ratio` directly (not re-porting it; the sketch's stated reason is written into the docstring: a model whose serving-time calibration leaves the same band its own promotion gate required is no longer the model that gate approved). `labeled_outcomes` is an injected iterable of `(error, variance)` pairs derived from HITL-labeled traces (the derivation from `hitl_queue` rows is a pure function taking rows as input, I/O stays with the caller, matching the `WatchlistLookup`/`PiDpmClient` convention). `CalibrationWatchResult` carries the ratio, the band, `in_band: bool`, and `response: Literal["none", "supervised_retrain"]` (the decision table's ~90% case, made explicit as data rather than prose). Unit: boundary cases at 0.8/1.2 exactly (reusing gate 3.7's boundary-test pattern), an in-band stream, an out-of-band stream, and an insufficient-data guard (`n < min_labeled`, returns `response="none"` with a stated reason rather than a confident verdict from 3 labels). Smoke: folded into gate 4.7's drill. Checksum: full suite + ruff.

4.3 Concept-drift proxies + decision table (gate F3). Three pieces, all pure:
- `serving/app/pidpm_client.py` additive contract extension, exactly as locked: the SageMaker payload may now carry `{"score": float, "epistemic_variance": float}`; `score`/`ascore` keep their `float | None` signature byte-for-byte (existing callers untouched, Phase 1-3 goldens unchanged), and a new `ascore_full`/`score_full` pair returns a frozen `PiDpmScore(score: float, epistemic_variance: float | None)`, with `epistemic_variance=None` whenever the payload omits it (which the current demo stand-in and any real checkpoint without an uncertainty head will). Unit: old-shape payload through new methods (variance None), new-shape payload through old methods (unchanged float), new-shape through new methods, plus the existing 8 client tests re-run untouched.
- `mlops/concept_proxy.py`: `flag_uncertain_trace(score, epistemic_variance, *, hitl_threshold, band_halfwidth, variance_floor) -> bool` (proxy 1: near-threshold AND high-variance) and `disagreement_rate(rows, *, hitl_threshold) -> DisagreementResult` (proxy 2: over labeled `hitl_queue` rows, model-implied verdict is `score >= hitl_threshold`, operator verdict is the label, `ambiguous` excluded exactly as pi-grpo's own `from_hitl_jsonl` excludes it; result carries the rate, n, and the excluded count so sparse data is visible, not silent).
- `mlops/drift_decision.py`: the sketch's decision table as one pure function `classify_drift(input_alerts, calibration, proxy1_trend, proxy2_trend) -> DriftDecision`, returning the category AND the response (`log_only` / `supervised_retrain` / `preference_pipeline`), including the cross-validation rule stated in the sketch: proxy 1 rising with proxy 2 flat is NOT concept drift (log only); proxy 2 rising is concept drift regardless of proxy 1. The disagreement alert threshold is the named placeholder from the sprint-honest scope above.
Unit: a truth-table test over the decision function (every row of the sketch's table plus the proxy-1-alone false-alarm row), plus proxy unit tests with hand-computed rates. Checksum: full suite + ruff; serving goldens byte-identical.

4.4 Preference-triple builder + storage (gate F4). New `mlops/preference_builder.py`, porting pi-grpo's two real entry points into the Harbormaster-extended triple schema locked in the sketch (`trace_id`, `mmsi`, `context{anchors, reasons}`, `chosen`/`rejected` arms with `source`/`verdict_or_trajectory`/`reward` (RewardBreakdown fields), `preference_source`, `hitl_operator`, `hard_violation_in_either_arm`, `created_at`):
- Vendored frozen dataclasses `RewardWeights` (hard=5.0, others 1.0) and `RewardBreakdown` (total/hard/soft/data/pref, `to_panel()`), byte-matching pi-grpo's shapes.
- `build_from_hitl(rows, contexts, *, hitl_threshold) -> list[PreferenceTriple]`: only disagreeing rows with `label in {"correct", "incorrect"}` (the real `from_hitl_jsonl` filter), chosen = operator side, rejected = model side (per the no-migration derivation above), `preference_source="hitl_verdict"`, `hitl_operator` from `reviewer`.
- `synthesize_from_reward(candidates, *, margin_min=0.5) -> list[PreferenceTriple]`: pi-grpo's real semantics (chosen = argmax `RewardBreakdown.total`, at most K-1 pairs, margin gate), `preference_source="reward_synthesized"`, `hitl_operator=None`.
- `hard_violation_in_either_arm` computed on EVERY triple (`RewardBreakdown.hard < hard_violation_threshold`, default 0.0) on both paths, specifically so gate 4.5's probe can audit the "5.0-weighted hard term prevents impossible winners" claim rather than assume it.
- Storage: `write_triples(dir, triples) -> Path` appending JSONL under `mlops/preference_data/` (gitignored except a committed fixture), mirroring `mlops/manifest.py`'s file-layout conventions; the S3 export is a printed command shape next to the gate 3.3 export, not a new uploader.
Unit: both construction paths against fixture rows (including: ambiguous rows dropped, agreeing rows produce no triple, a synthesized pair where the lower-total arm has the hard violation, margin-gate boundary); schema round-trip (write then parse, every required key present); a deliberate-invariant test that a kinematically-violating arm with inflated soft/data/pref can still lose on total under the 5.0 hard weight, and that when it manages to win anyway `hard_violation_in_either_arm=True` is recorded (the probe's input). Smoke: `make preference-smoke` builds triples from the committed fixture and prints counts per source. Checksum: SHA256 of the fixture's expected triple count + first-triple hash pinned in `mlops/fixtures/preference_expectations.json`.

4.5 Reward-hacking probe + promotion wiring (gate F5). New `mlops/reward_hacking_probe.py` implementing the locked sketch verbatim: `RewardHackingProbeResult` (frozen: baseline/candidate mean rewards, baseline/candidate hard-violation rates, `blocked`, `reason`) and `run_reward_hacking_probe(baseline_rewards, candidate_rewards, *, hard_violation_threshold=0.0)`; blocking condition is exactly `candidate_mean_reward > baseline_mean_reward AND candidate_hard_violation_rate > baseline_hard_violation_rate`, everything else passes. Wiring into `mlops/promote.py`, an addition not a redesign: `run_promotion` gains keyword-only `reward_hacking_result: RewardHackingProbeResult | None = None`, checked immediately after the holdout-gate step and before shadow (a blocked probe appends `PromotionStep(stage="reward_probe", action="fail")` and halts exactly the way a failed gate halts); `None` means not-a-DPO/GRPO-candidate and skips the step entirely, so the existing pinned clean-run transition sequence in `mlops/fixtures/expectations.json` is unchanged and every existing promote test passes untouched (that is itself an assertion, not a hope: the gate's test list includes re-running them). Unit: probe truth table (both-up blocked; reward up violations flat/down passes; reward down passes regardless; empty-stream guard), wiring tests (blocked probe never reaches shadow, passing probe proceeds, None skips, step bookkeeping records `reward_probe` only when a result was supplied). Checksum: existing `expectations.json` byte-identical; full suite + ruff.

4.6 Drift-alerting Terraform, plan-only (gate F6). DONE 2026-07-04. `modules/drift_watch` behind `enable_phase4` (whole-module `count` at the env call site, the `emr_backfill` convention, no `enable_phase1` requirement since the module depends only on Phase 0's lake bucket + finops SNS topic): an EventBridge Scheduler schedule (`rate(1 day)`, input drift is a slow population signal, unlike Phase 2's 1-minute replication-lag check) invokes a Lambda (`infra/lambda/drift_watch/handler.py`, packaged by `make drift-lambda-package`, mirroring `cdc-lambda-package`'s vendoring pattern: copies `handler.py` + vendors `mlops/__init__.py` + `mlops/drift.py` + pip-installs pandas/pyarrow into the build dir) that reads two parquet snapshots from the lake bucket via S3, calls the gate 4.1 `check_input_drift`, and publishes to the existing Phase 0 finops SNS topic on any drifted feature (no new topic). No VPC (unlike `cdc_monitoring`): S3 and SNS need no VPC attachment, so this module has zero standing cost beyond the schedule/Lambda/log-group themselves (both effectively free at this invocation rate). IAM scoped to `s3:GetObject` on exactly the two snapshot keys and `sns:Publish` on the one topic ARN, nothing else. NOT applied this sprint. Unit: `summarize_drift` (the pure decision path) tested directly, plus `handler()` end-to-end against fully stubbed boto3 S3/SNS clients and real parquet bytes (5 tests, `infra/lambda/drift_watch/test_handler.py`, kept out of `testpaths` matching `infra/lambda/teardown`'s own precedent for Lambda-runtime code). Smoke: `make validate` clean. Checksum: untargeted `terraform plan` with committed values (all toggles false) attributes 0 add / 2 change (the pre-existing finops drift) / 0 destroy; untargeted plan with `enable_phase1=true, enable_phase4=true` (no `-target` needed at all, thanks to this sprint's `ecs_serving` count fix) shows exactly 8 to add (IAM role + policy + policy attachment for the Lambda, the Lambda function, its log group, the scheduler's IAM role + policy, the schedule itself), 0 change beyond the same finops drift, 0 destroy. Real finding, recorded honestly rather than silently accepted: `make drift-lambda-package`'s build dir measures 229MB unzipped (pyarrow's bundled Arrow C++ libs alone are 123MB) against Lambda's 250MB unzipped limit - real headroom, but tight, and never actually deployed to confirm AWS's own reported size; noted in `infra/lambda/drift_watch/requirements.txt` as a real risk for whoever applies this module for real (a Lambda Layer is the standard fix if it comes up short).

4.7 Drills + acceptance e2e + closeout (gate F7, the phase gate). Drill L3 (`scripts/drill_l3_drift_classification.py`, transcript `docs/drills/L3_drift_classification.md`): synthetic streams drive the REAL `mlops` code (not reimplementations) through all three decision-table rows plus the false-alarm row: input-only drift logs and does nothing; a calibration excursion outside [0.8, 1.2] routes to `supervised_retrain`; a rising disagreement rate routes to the preference pipeline and `build_from_hitl` emits valid triples; proxy 1 rising alone does NOT trigger retraining. Drill L4 (`scripts/drill_l4_reward_hacking.py`, transcript `docs/drills/L4_reward_hacking_probe.md`): a deliberately gamed synthetic candidate (higher mean total AND higher hard-violation rate) is blocked inside the real `run_promotion` call before shadow; an honest candidate (higher total, violations equal-or-down) passes the probe and proceeds; both assertions in one exit-0-only-on-contrast script, the gates-3.8 pattern. War stories P13 (cross-validated drift proxies prevent a false concept-drift retrain) + P14 (reward-hacking probe blocks a gamed checkpoint) appended to `PLATFORM_WAR_STORIES.md` and mirrored to `arunshar/debug-war-stories` per the standing upkeep rule. `tests/e2e/test_phase4.py` (unguarded, ordinary suite, the Phase 3 precedent: everything is pure functions, a skip flag would only hide assertions) encoding the acceptance criteria below. Closeout: full suite + ruff + `make validate` + this doc's status line and the master plan updated with real results.

## Acceptance criteria (the phase gate)

- (a) Input drift: the PSI/KS port alerts on the drifted fixture, stays silent on the undrifted one, and matches MIRROR-computed expected values numerically.
- (b) Calibration drift: an out-of-band labeled stream returns `supervised_retrain`; boundary values behave exactly as the Phase 3 holdout gate's band does.
- (c) Concept drift: rising disagreement routes to the preference pipeline and produces schema-valid triples with `hard_violation_in_either_arm` computed; proxy 1 rising alone does not trigger retraining.
- (d) Reward hacking: a gamed candidate is blocked inside `run_promotion` before shadow; an honest improvement passes; the pre-Phase-4 pinned promotion sequence is byte-identical when no probe result is supplied.
- (e) Toggle hygiene: `enable_phase4=false` (committed value) attributes zero Terraform diff to Phase 4.

## Cost envelope (us-east-1, $75 cap)

$0 this sprint: every gate is local pure-Python; gate 4.6 is authored and plan-verified only. When the drift Lambda is eventually applied (post-sprint, Arun-run): EventBridge schedule free tier, one small Lambda invocation per interval (cents/month), SNS existing topic, S3 reads of two parquet snapshots per run (cents). No new standing cost surfaces; the module sits behind `enable_phase4=false` like every other phase toggle.

## War stories (produced by this phase, grounded only when hit)

- P13 drift-proxy false alarm averted [PLATFORM / personal-build, ML-RELIABILITY]: near-threshold+high-variance volume rises (proxy 1) while HITL disagreement stays flat, the decision table correctly reads "more ambiguous traffic, model still right" and does NOT retrain; grounded by drill L3.
- P14 reward hacking blocked [PLATFORM / personal-build, RL-SAFETY]: a candidate raises mean total reward by exploiting soft/data/pref terms while its hard-violation rate rises; the probe blocks promotion before shadow; grounded by drill L4.

## Scope guard (do not drift)

Phase 4 is the drift -> HITL -> preference-data flywheel plus one promotion-gate addition. It is NOT: the MSI DPO/GRPO training loop itself (MSI-side, post-sprint); a HITL frontend change (corrected-trajectory capture explicitly deferred); a live-applied monitoring stack (gate 4.6 stays plan-only this sprint); a redesign of `mlops/promote.py` (one optional argument, one step); a `hitl_queue` schema migration (derivation over migration, per the authoring finding above). Per the standing master-plan scope guard: no query routing, no consensus implementation.

## Reuse anchors (do not rebuild)

`mlops/holdout_gate.py:calibration_ratio` (re-called directly, never re-ported). `mlops/promote.py` (the state machine, gains one optional step). `mlops/manifest.py` (file-layout conventions for `preference_data/`). `serving/app/hitl.py` (`hitl_queue` DDL + `FeedbackIn`, read as-is, no migration). `serving/app/config.py:anomaly_hitl_threshold` (the single threshold both proxies and the triple builder share). `serving/app/pidpm_client.py` (gate 3.6 client, additively extended). `tests/e2e/test_phase3.py` + `lake_helpers.py` (unguarded-e2e pattern). Makefile drill/target conventions (gates 3.8/3.9). External, ported not imported, real paths verified 2026-07-04: `~/code/mirror/serving/monitoring/drift.py` (PSI/KS/DriftConfig, numpy-only); pi-grpo at `~/Library/CloudStorage/Dropbox/Claude Interview Prep Documents/repo-audit-work/pi-grpo/app/components/` (`preference_builder.py`: PreferencePair/from_hitl_jsonl/synthesize_from_reward semantics; `physics_reward.py`: RewardWeights/RewardBreakdown/PhysicsReward.score).

## Adversarial review addendum (2026-07-04, 4-lens pass: correctness, security, Terraform/cost, test-coverage)

Fixed in this pass:
- **`mlops/preference_data/` had no `.gitignore` entry and no committed fixture, despite gate 4.4's own text promising both.** `write_triples()` persists real `trace_id`/`mmsi`/`reviewer` values there; without an ignore rule, a routine `git add -A` on this personal repo could commit real HITL data to history. Added the ignore rule (`.gitignore`), plus the promised fixture/smoke pair: `mlops/fixtures/preference_hitl_rows.json` (6 synthetic rows exercising all three exclusion paths: agreement, ambiguous/unlabeled, and the score-consistency guard below), `mlops/fixtures/preference_expectations.json` (pinned triple count + first-triple hash, same convention as `drift_expectations`-style checksums), `scripts/preference_smoke.py`, and a `make preference-smoke` target. Verified: `make preference-smoke` passes, full suite still 373 passed / 9 skipped, ruff clean.
- `PHASE_4.md`'s own status line was corrected on this branch already by gate 4.7's closeout commit (CODE-COMPLETE, signed off, matching the master plan). The stale "AUTHORED... pending sign-off" draft only persists on `phase3-lake`/`master` because this branch hasn't merged back yet; normal unmerged-branch divergence, not a process violation; worth knowing before reading `PHASE_4.md` from any branch other than this one.
- **The reward-hacking probe's magnitude-blind gap (below, "boundary-riding soft-term hacker") is FIXED, signed off by Arun 2026-07-04.** `mlops/reward_hacking_probe.py`'s blocking condition is now `blocked = mean_up and (rate_up or hard_degraded)`, where `hard_degraded = candidate_mean_hard < baseline_mean_hard`; no new dataclass fields, no new thresholds, exactly the minimal fix proposed. The predicted blast radius held exactly: `test_custom_hard_violation_threshold_is_respected` is the one existing test whose expected outcome flipped (a reward-up candidate whose hard term slips now correctly blocks, for both threshold values tested), updated with an explanatory comment rather than removed. The strict-xfail marker on `test_probe_should_block_a_boundary_riding_soft_term_hacker` is removed (renamed to `test_probe_blocks_a_boundary_riding_soft_term_hacker`) since it now genuinely passes. Verified: full suite 375 passed / 9 skipped (was 374/9/1 xfail), ruff clean.

Documented, not changed (the underlying code is correct and already tested; the design intent just wasn't visible above the module-docstring level):
- **`disagreement_rate` (`mlops/concept_proxy.py`) and `build_from_hitl` (`mlops/preference_builder.py`) both silently exclude a row where `label == "incorrect"` but `score < hitl_threshold`**, on top of the documented ambiguous/unlabeled exclusion, merging both reasons into one `excluded` count. This is real and intentional (module docstring: "a row where [the model-implied verdict] does not [hold] is excluded as a data inconsistency rather than silently miscounted"), and it is tested (`test_disagreement_rate_excludes_rows_below_the_model_implied_threshold`, `test_build_from_hitl_drops_rows_below_the_model_implied_threshold`): not a bug. But this doc's gate 4.3/4.4 prose never mentioned the guard, only "ambiguous excluded," so a reader working from this doc alone would not expect it. Flagging here so the doc matches the code; if the merged `excluded` count ever needs to distinguish "genuinely ambiguous" from "score went stale," that's a real (if minor) future enhancement, not a fix.

Flagged, left for a future pass (none block gate 4.6 ever being applied, none are urgent for a $75/mo personal account):
- ~~Weak adversarial coverage on the reward-hacking probe.~~ **RESOLVED, see "Fixed in this pass" above.** The follow-up test found a real gap (the boundary-riding soft-term hacker), and the proposed fix is now applied and signed off, not just proposed.
- **`phase4-flywheel` forked from an older point on `phase3-lake`** (`a9712fe`) and is missing 3 Flink fixes `phase3-lake` picked up afterward (rolling-history-window gap, worker-side deps/TTL/schema, real PyFlink packaging). Not scope drift (none of Phase 4's own commits touch `streaming/flink/`), just needs a rebase before this branch is treated as mergeable.
- **Terraform's own "0 add" / "exactly 8 to add" plan-checksum claims have no committed artifact** (no `.tfplan`, no plan-output fixture) to back them, unlike every other gate's numeric claims, which are pinned in a fixtures file. A pre-existing repo-wide gap (Phase 3's Terraform checksums have the same gap), not a Phase-4 regression; close it by pasting a real `terraform plan` transcript into this doc the next time this branch is touched, before ever running `apply`.
- Gates 4.0-4.6 were only CI-verified in aggregate at the final commit (one workflow run, tied to `191f79b`), not gate-by-gate the way Phase 3's CI-vs-local gap-finding was. Low risk (final state is green, 373 passed), but if an intermediate commit had transiently broken CI it would not have been caught.
- `mlops/drift.py`'s ported PSI degenerates to `0.0` (never-drifted) when the reference column is (near-)constant, inherited byte-for-byte from MIRROR's original module, not a Phase 4 regression. Worth knowing if this is ever pointed at a near-constant/quality-flag-style feature column.
- `classify_drift`'s real keyword args (`input_results`, `calibration`, `disagreement`) differ from this doc's gate 4.3 prose (`input_alerts`, `calibration`, `proxy1_trend`, `proxy2_trend`). Tests use the real signature, so this is a doc-readability gap only.
- The packaged drift-watch Lambda (pandas + pyarrow + numpy vendored) measures 229MB unzipped against Lambda's 250MB limit, never deployed to confirm it fits after AWS-side compression/layering. Already self-flagged in `infra/lambda/drift_watch/requirements.txt`; carried forward here as a real pre-apply risk, not a new finding.
- `infra/lambda/*` (including this phase's own `drift_watch/test_handler.py`) is excluded from `pyproject.toml`'s `testpaths` and from the CI workflow's ruff-check target list entirely, matching Phase 2/3's existing convention, not a new regression. Live consequence found during this review: `test_handler.py` currently fails `ruff check` (unsorted imports) and this has never been caught, since nothing runs ruff against `infra/lambda/` in CI. Worth closing (either lint `infra/` too, or accept the gap explicitly) the next time this branch is touched.

Confirmed clean, no complaint: `mlops/drift_decision.py`'s full 7-row truth table (including the "proxy 1 alone is not concept drift" cross-validation rule) traced and correct; `run_reward_hacking_probe`'s blocking condition is exactly the documented strict `>`/`>` on both terms, with safe empty-stream defaults; `run_promotion`'s new step is genuinely additive (existing pinned `clean_run`/`rollback_run_burns_at_25` sequences byte-identical, explicitly re-tested with `reward_hacking_result=None`); `build_from_hitl`'s chosen/rejected assignment and `synthesize_from_reward`'s margin-gate/K-1-pairs logic are correct; the drift port is numerically verified against MIRROR's real module, not just self-consistent; Terraform IAM in `modules/drift_watch` is scoped to exactly the 2 S3 keys + 1 SNS topic claimed, no wildcards; no secrets or real account IDs in the diff; cost is genuinely cents/month (daily schedule, not per-minute, bounded Lambda timeout/memory, 14-day log retention); no duplicate SNS topic; `epistemic_variance` cannot influence the reward-hacking probe (architecturally isolated, traced the full data path); CI on this branch's final commit is genuinely green (373 passed / 9 skipped, ruff clean), so Phase 3's missing-pip-extras CI gap did not recur.
