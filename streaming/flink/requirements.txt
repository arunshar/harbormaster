# Managed Flink (KDA) feature-job runtime deps. The KDA Flink 1.20 runtime bundles
# the Kinesis connector; apache-flink is pinned so the artifact packages against
# the matching PyFlink API, and boto3 backs the DynamoDB feature-store sink.
apache-flink==1.20.0
boto3>=1.34
